import { Component } from '@angular/core';

@Component({
  selector: 'my-app',
  template: '<h1>My First Angular 2.0 App</h1>'
});

export class AppComponet {}

/*
* Every Angular app has at least one root component,
* conventionally named AppComponent, that hosts the client
* user experience. Components are the basic building blocks
* of Angular applications. A component controls a portion of
* the screen — a view — through its associated template.
*/
